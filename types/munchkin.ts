export type Player = {
  id: string;
  name: string;
  gender: string;
  level: number;
};

export type Rule = {
  id: string;
  cardName: string;
  ruleDescription: string;
  createdAt: string;
  updatedAt: string;
};
